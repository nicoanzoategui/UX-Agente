Scripts opcionales de desarrollo (ejecutar desde la carpeta `backend` con `npx tsx scripts/<archivo>.ts`):

- `test-gemini.ts` — prueba rápida de la API de Gemini.
- `list-models.ts` — lista modelos disponibles.
- `db-dump.ts` — imprime tablas `kickoff_cards` y `kickoff_wireframes`.
